// this is documentation section of the C++ where 
// write these comments usually to describe the program
// point out the working and requirement


#include<iostream> // this is linking section
// where we link other c++ files which can be accessed 
// we can access from the system library or we access other
//  c++ files
using namespace std; // so that we not use std in every comand lines or variables.
// this section is where we define some of the variables their primary values
// like pi=3.14 or sth to be applied in whole program instead of defining per function or line.

//also we can declare some global variables (ig we have limited use of such programs)

// we also define class which will be our backbone to our programing in oop


int main() // main function. its like the cabin of the ship. ever function must be executed in main 
// as it is responsible of runing the program 

// every c++ program starts with a main() so it is called entry point function.

{
	cout<<"Hello world";
	return 0;
}