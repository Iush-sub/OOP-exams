// character set is the set of characters like alphabet numbers and symbol that is recognized or allowed
// by c++. mostly everything ig there is no particular domain in ths
// the token is the important thing which is nth but the basic elements 
// recognized by the c++ compiler
// keywords(the words reserved by the programing language)
// Identifiers(name of variables)
// variables(those characters which are responsible to held a value of a certain datatype)
// string (collection of some characterset enclosed in a doubel quotas )
// operators (symbols which perform logical and mathematical calculation)
// constant (whose value is fixed and is unlikerly to change throughout the program)
 
